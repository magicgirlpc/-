# 作品集网站项目上下文

## 协作规则

- 这是长期持续开发的个人作品集网站。保持已确认的页面结构、视觉风格、交互逻辑和资源路径，不因对话变长而重新猜测。
- 优先使用 `rg`、定点 `sed`、`git diff` 和局部读取；不要反复完整读取大型数据文件，尤其是 `app/pacome/data.ts`。
- 需要查找作品、URL、styleframes 或字段时，先搜索定位，再只读取相关片段。
- 每个重要阶段完成后更新本文件；若上下文发生压缩，先读取本文件恢复状态。
- 未经用户明确要求，不打开浏览器、弹出面板或进行额外界面操作。
- 以后每次向用户回复时，在末尾附上当前作品集链接 <http://localhost:3000/>，以及原版英文作品集链接 <https://pacomepertant.com/>。

## 项目位置与关键文件

- 仓库根目录：`/Users/hepevsmbp-2/Documents/Codex/个人集/portfolio`
- 作品数据：`app/pacome/data.ts`
- 作品详情组件：`app/pacome/ProjectDetail.tsx`
- 详情页样式：`app/projects/project-detail.css`
- 静态资源：`public/portfolio/`
- 固定参考网站：<https://pacomepertant.com/>

## 已确认的设计要求

- 自动播放视频保持完整 16:9，不裁切。
- 所有作品图片与详情页图板按素材原始横竖比例展示，不统一套用 16:9；只有视频遵循其实际视频比例（项目 14 视频为完整 16:9）。
- B 站 / 新片场链接是参考网站原链接，不能当作嵌入视频源；外链卡片使用对应平台的“前往B站播放”或“前往新片场播放”按钮。
- 图片保持原始横竖构图；横图与竖图分组排列，删除图片后自动补位。
- 不出现单独放大的图片或由此产生的大面积空白。项目 12 的 12 张静帧固定为 3 行 × 4 张，尺寸一致。
- 作品从 Spiral 进入并关闭后，应回到刚才选中作品附近继续播放，而不是跳回 01。

## 当前接入：项目 16

项目名称：`16-Sony store宣发物料设计`

- 跳过项目 15；已将用户提供的 22 张压缩照片按“门店现场 → 线下物料 → 产品/功能说明 → 视觉方案”的阅读顺序整理。
- 文件位于 `public/portfolio/project-16/gallery/01.jpg` 至 `22.jpg`，全部放入同一个作品静帧网格，不拆分区。
- 封面改为用户提供的 `public/portfolio/project-16/cover.png`（1672×940）。
- 详情页将横构图上下堆叠，旁侧竖构图跨两行对齐；图片保持完整显示并使用更紧凑间距。
- `app/pacome/data.ts` 与 `app/pacome/ProjectDetail.tsx` 已完成项目 16 接入，所有 22 张图片可点击放大。

## 最近完成：项目 14

项目名称：`14-WH-1000XM5玫瑰灰宣发`

- 封面：`public/portfolio/project-14/cover.png`；2026-09-01 已替换为用户提供的 1672×940 PNG 附件。旧的 `cover.jpg` 及构建目录中的对应副本已删除。
- 自动播放视频：`public/portfolio/project-14/video-preview.mp4`；2026-09-01 已替换为用户提供的新附件，22 秒、854×480、H.264/AAC、约 6.3 MB，保持完整 16:9，不裁切。
- 新片场外链：`https://www.xinpianchang.com/a13419156?channel=copyLink&from=webShare`，CTA 文案为“前往新片场播放”。
- 静帧：`public/portfolio/project-14/styleframes/01.jpg` 至 `08.jpg`，详情页按 2 列整齐网格展示，竖构图保持完整比例并可点击放大。
- 新增产品图：`public/portfolio/project-14/product/01.jpg` 至 `09.jpg`，详情页中以 3 列 × 3 行网格展示，竖构图保持完整构图。
- `app/pacome/data.ts` 已接入项目 14 的封面、预览视频、新片场外链和 8 张静帧。
- `app/pacome/ProjectDetail.tsx` 已设置项目 14 外链 CTA，并为竖构图静帧保持 9:16 完整显示。

## 既往完成：项目 13

项目名称：`13-A9M3创作者故事-那岩`

- 封面：`public/portfolio/project-13/cover.jpg`
- 静帧：`public/portfolio/project-13/styleframes/01.jpg` 至 `08.jpg`
- 自动播放视频：`public/portfolio/project-13/video-preview.mp4`
- 新片场外链：`https://www.xinpianchang.com/a13418516?channel=copyLink&from=webShare`
- `app/pacome/data.ts` 已加入项目 13 的封面和静帧，并设置静帧标签“作品静帧”。
- `app/pacome/ProjectDetail.tsx` 已让项目 13 使用统一静帧网格，不采用首张/前两张放大逻辑。
- `app/projects/project-detail.css` 已为项目 13 的移动端静帧设置两列布局和较小间距。

## 既有作品处理要点（摘要）

- 项目 07：`7-INZONE 品牌广告片`，主封面、自动播放视频、静帧和次视频已整理。
- 项目 08：`8-HT-AX7 积木音箱`，含主封面、自动播放视频、静帧、分镜手稿和产品使用场景图板块。
- 项目 09：`9-当山真玲-亚洲巡演VLOG`，含片头自动播放视频、静帧、舞台照和演出照板块；图片按横竖构图整理。
- 项目 10：`10-橘子海乐队专访`，含封面、自动播放视频和静帧。
- 项目 11：`11-PS-LX5BT、3BT宣发`，含封面、自动播放视频、视频静帧、产品图拍摄和拍摄方案；“拍摄方案”标题已移除。
- 项目 12：`12-音频大师说-向征、王子建、胖雪人`，含自动播放视频、B 站外链卡片与 12 张统一静帧网格（3×4）。

## 验证记录

- 2026-09-01：项目 14 新增 9 张产品图，按 3 列 × 3 行网格接入；构建通过，仅有既有的 lottie direct eval warning。
- 2026-09-01：项目 14 标题更新为“WH-1000XM5玫瑰灰宣发”，静帧暂时撤下；构建通过，仅有既有的 lottie direct eval warning，无新增错误。
- 2026-09-02：项目 14 接入 8 张竖构图静帧，按整齐网格展示且保持 9:16 完整比例；构建通过，仅有既有的 lottie direct eval warning。
- 2026-09-02：项目 16 更新封面，并将横竖图改为紧凑对齐网格；竖图跨两行与横图总高度对齐，保留懒加载和点击放大。
- 2026-09-02：项目 17 接入主 B 站外链“2024索尼电视及家庭影院新品揭晓”和次要 B 站外链“2026索尼家庭影音娱乐新品发布会回顾”，分别使用用户提供的两张封面；项目主封面后续替换为用户提供的 BRAVIA、TURE RGB 新品发布会合成图；项目页隐藏无内容的静帧占位区。
- 2026-09-02：项目 18 接入主 B 站外链“索尼英纵电竞新品发布会 | 天生为赢”，以及“发布会回放”和“INZONE好物推荐”两个次要 B 站外链；已分别使用用户提供的三张主封面、回放封面和推荐封面，项目页隐藏无内容的静帧占位区。
- 2026-09-02：项目 18 接入 8 张横向静帧，桌面端按 4 列 × 2 行展示，移动端按 2 列展示；保留懒加载与点击放大。
- 2026-09-02：取消项目 18 静帧前两张的单独放大规则，8 张图片统一作为普通卡片严格按 4 列 × 2 行排列。
- 2026-09-02：项目 19 接入索尼官网商业案例主链接及两个次链接，使用用户提供的封面；官网外链卡片显示“查看案例”，项目页隐藏无内容的静帧占位区。
- 2026-09-02：项目 19 继续新增 3 条索尼官网商业案例外链：文化展览空间、周大福零售空间、交通显示设备；用户重复提供的主链接未重复添加。
- 2026-09-02：项目 19 追加 B 站外链“索尼发布电影感直播新方案：无界商显”，使用项目19封面作为卡片回退图。
- 2026-09-02：项目 19 在“家庭影院方案”之后、“文化展览空间”之前插入官网案例“企业显示设备”，其余外链保持不变。
- 2026-09-02：新增项目 22，命名为“2025反恐精英亚洲邀请赛”，暂使用标准占位结构，等待后续素材与链接。
- 2026-09-02：项目 22 接入用户提供的封面和 B 站主外链“索尼INZONE英纵助力CAC赛场名场面！”，项目页隐藏无内容的静帧占位区。
- 2026-09-02：项目 21 接入用户提供的 10 张 ULT FIELD 3、5 静帧，桌面端按 2 列 × 5 行排列，取消首图单独放大规则并接入主 B 站评测外链。
- 2026-09-02：项目 21 更新为用户提供的 ULT FIELD 3、5 评测封面。
- 2026-09-02：项目 15 更新为用户提供的 CH530&CH730 总宣发封面。
- 2026-09-02：项目 22 新增 10 张用户提供的 CAC 静帧，桌面端严格按 5 列 × 2 行排列，所有图片保持同规格卡片且不使用单图放大布局。
- 2026-09-02：项目 20 接入用户提供的封面、1 条主 B 站链接、7 条次要 B 站链接和 1 条新片场链接；外链按平台显示对应播放按钮，项目页隐藏无内容的静帧占位区。
- 2026-09-01：项目 16 接入 22 张压缩照片，跳过项目 15，按阅读顺序置于单一静帧网格；构建通过，仅有既有的 lottie direct eval warning。
- 2026-09-01：修正项目 16 混合横竖图网格的行高拉伸问题，横图与竖图均按原始比例显示，不再出现空白或裁切；构建通过。

- 2026-08-31：项目 13 构建通过。使用项目依赖环境执行：

  `CI=true PATH="/Users/hepevsmbp-2/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:/Users/hepevsmbp-2/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/fallback:$PATH" pnpm run build`

- 构建仅有既有的 lottie direct eval warning，无新增错误。

## 最近完成：首页封面加载与动画性能优化

- 保留 `public/portfolio/project-*/cover*` 原始封面不变；为首页螺旋/列表预览新增 22 张 `cover-optimized.jpg`，统一限制为 960×540、JPEG 质量 82，详情页继续使用原始封面。
- `PacomeProject.previewImage` 只供首页动效封面使用，避免首页首屏下载和解码 1–2MB 的 PNG 或 4000px 原图。
- 螺旋首页只预载当前卡片前后约 2.5 个位置的封面，并将当前卡片设为高优先级，其余邻近卡片低优先级；列表悬停预览同样使用轻量副本。
- 移除螺旋动画每一帧的 `sessionStorage` 同步写入，改为交互停止后延迟保存；自动旋转按真实时间推进并限制约 60 次/秒绘制。
- 为螺旋卡片增加绘制隔离和合成层提示，降低移动端频繁 transform、透明度和模糊效果引起的重绘压力。
- 2026-09-04：`pnpm run build` 通过，仅保留既有的 lottie direct eval warning；浏览器回归确认入口、螺旋视图和列表视图均可进入与切换。

## 最近完成：螺旋自转与界面音效

- 恢复 spiral 自动旋转，视觉速度保持约 `0.108` 个项目/秒（约 9.26 秒经过一张）；改为按实际经过时间计算并限制约 60 次/秒绘制，避免 120Hz 设备转速与渲染量翻倍。
- 自转保留轻量封面、邻近加载、绘制隔离和位置存储节流；页面进入后台时暂停，回到前台继续，拖拽期间由指针事件直接绘制。
- 用户提供的 6 条交互音效已压缩为 96kbps MP3，背景循环已从约 6.37MB / 320kbps 压缩为约 1.59MB / 80kbps；源文件未修改。
- 新增 `app/pacome/sound.ts`，负责音效缓存、状态持久化、背景循环淡入淡出和后台暂停；背景音量设为 7%，并使用 `preload="none"` 避免抢占首页封面带宽。
- 进入作品详情时沿用同一个背景播放器，并在约 420ms 内降至 2.2%；关闭详情后约 650ms 恢复至 7%，不暂停、不归零。播放时间同时写入会话存储，路由重载时也可从之前的位置续播。
- 音效接入位置：进入主界面、桌面端作品卡片悬停、打开作品、返回首页、菜单展开/关闭、右下角声音开关；作品详情与首页共享同一声音状态。

## 待办

- 项目 14 已完成接入；其余部分不做额外优化。

## 最近完成：项目 10 静帧顺序调整

- 项目 10 的作品静帧移除 `01.jpg`；将 `16.jpg`、`17.jpg`提前为开场两张大图；`02.jpg`改为半宽并与`03.jpg`并排，其余静帧保持原顺序。

## 最近完成：首页简历入口

- 菜单项目调整为“作品 / 简历 / 联系方式”，移除原版残留的 Ig、X、Be、Li 社交链接。
- 用户提供的三页工作简历原样复制至 `public/resume.pdf`；菜单“简历”和首页左下黄色卡片均在新标签页打开该文件。
- 黄色卡片上方新增“查看简历”悬浮引导，使用向下箭头指向卡片，并兼顾移动端尺寸及减少动态效果偏好。

## 最近完成：站内简历预览与联系方式窗口

- 首页黄色卡片和菜单“简历”不再直接打开浏览器 PDF，而是在站内打开可关闭的三页图片预览；移动端可直接上下滑动，顶部提供原 PDF 下载按钮。
- 新增菜单内联系方式窗口，集中展示邮箱 `1579713724@qq.com`、手机号 `17631087716` 和用户提供的微信二维码；关闭窗口后保留并返回上一层菜单。
- PDF 预览图为 120 DPI 渐进式 JPEG，二维码使用原始附件，避免移动端加载完整 PDF 后难以返回作品集。

## 最近完成：导航与点击反馈修正

- 菜单中“作品 / 简历 / 联系方式”统一使用相同字号、字重、字距和行高，消除按钮全局样式造成的视觉差异。
- `spiral / list` 切换按钮补上切换音，首页左下简历卡补上打开音。
- 欢迎页的 `Enter my portfolio` 改为在用户点击的同一手势中启用声音并播放进入音，避免移动端浏览器拦截延迟播放。
- 已从原版公开打包脚本中拆出四套 Logo Lottie，左上 Logo 按 `face1 → face3 → face4 → face5` 循环；每次点击会切换原版表情、从头播放非循环动画并播放提示音。单套数据约 60–66KB，按需加载。
- 2026-09-05：构建通过（仅保留既有 lottie direct eval warning）；浏览器回归确认欢迎页可进入、Logo 状态可切换、视图可切换且三个菜单标题视觉一致。
- 2026-09-05：修复背景音渐变首帧可能产生负数音量的 `IndexSizeError`；渐变进度和音量现均限定在合法范围，重叠渐变会作废旧帧。同时增加背景音自动恢复，浏览器拦截路由恢复播放时，下一次用户交互会再次启动。构建及首页→详情→首页回归通过。

## 最近完成：螺旋延展与拖动体验

- 核对原版脚本后确认，原版通过创建两轮完整项目平面来保证螺旋上下连续。本地版保留单轮轻量 DOM，改为根据实际视口高度计算需要的封面范围。
- 中心封面立即加载，上下外围可见封面低优先级分批加载；同时减轻远端模糊和透明度衰减，不再出现只有中间有图、上下空缺的情况。
- 螺旋场景、链接和图片三层均禁用浏览器原生拖放与文本选择，拖动作品时不再出现图片幽灵、`copy` 标记或页面变蓝。
- 首页左下“查看简历”悬浮引导整体上移，与 `2023–2026` 环形文字拉开距离，箭头仍指向黄色简历卡。
- 2026-09-05：`pnpm run build` 通过（仅保留既有 lottie direct eval warning）；桌面端浏览器回归确认螺旋顶部、中部和底部均有连续项目卡片。
- 2026-09-05 细节润色：将螺旋相邻卡片的角度收紧至原版提取值 `0.85rad`，纵向步距从 84px 收紧至 72px（移动端 52px）；中心卡片继续清晰突出，外围卡片按景深和离中心距离逐渐模糊、降低透明度并略微缩小，减少“照片墙”感。
- “查看简历”引导线由 82px 缩短至 48px（移动端 42px），箭头停在环形文字上方且仍指向黄色卡片；构建与桌面端视觉回归通过。

## 最近完成：触控板响应与作品转场

- 保留普通鼠标滚轮的原版 `deltaY * 0.00015` 反馈；对 Mac 触控板常见的像素级小幅连续输入单独提高增益至 `0.00042`，并增加小幅最低冲量，避免手势被自转最低速度与惯性衰减抵消。
- 参考原版点击后先执行 `hideProjects()` 的节奏，本地 DOM 版点击作品后会暂停螺旋，让其他卡片模糊退场、目标卡片轻微向前，再用浅色圆角幕布衔接路由。
- 详情页入场改为同色幕布向上揭开，项目卡片从轻微下移和缩小状态回到原位，关闭按钮延迟淡入；减少动态效果偏好下会直接切换。
- 2026-09-05：构建通过（仅保留既有 lottie direct eval warning）；浏览器回归确认螺旋响应、退场幕布、详情页揭幕和路由均正常，控制台无错误。
